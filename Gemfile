source "https://rubygems.org"

# Specify your gem's dependencies in bored_solo_cli.gemspec


gem "rake", "~> 12.0"
gem "rake", "~> 12.0" (this one should already be in there)
gem 'pry'
# * require json * and rest-client when you are using an API
gem 'json' 
gem 'rest-client'
